return function ()
    local datahandle = require(game.ReplicatedStorage.DataHandleModule).new()
        print(datahandle.Cache,"data cache data cache data cache dtatacachedatacachedatache")
    -- datahandle.DataChanged:Connect(function(data)
    --     print(data)
    -- end)
end